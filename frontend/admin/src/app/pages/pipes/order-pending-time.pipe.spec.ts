import { OrderPendingTimePipe } from './order-pending-time.pipe';

describe('OrderPendingTimePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderPendingTimePipe();
    expect(pipe).toBeTruthy();
  });
});
