import { OsKeyboardDirective } from './os-keyboard.directive';

describe('OsKeyboardDirective', () => {
  it('should create an instance', () => {
    const directive = new OsKeyboardDirective();
    expect(directive).toBeTruthy();
  });
});
