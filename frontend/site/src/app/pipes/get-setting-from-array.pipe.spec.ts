import { GetSettingFromArrayPipe } from './get-setting-from-array.pipe';

describe('GetSettingFromArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSettingFromArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
